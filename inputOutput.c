//
//  inputOutput.c
//  
//
//  Created by Matthew on 09/03/2021.
//

#include <stdio.h>
/* prints the input */

/*
main() {
    int c;
    printf("%d", EOF);
    c = getchar();
    while ((c=getchar()) != EOF)
        putchar(c);
    
}
*/

/* count characters in input*/
/*
int main() {
    long nc;
    
    while (getChar() != EOF)
        ++nc;
    printf("%ld\n", nc)
}
/*

/* count lines in input */
int main() {
    int c, nl;
    
    nl = 0;
    while ((c = getChar()) != EOF)
        if (c == '\n')
            ++nl;
    printf("%d\n", n);
}   

/* Exercise 1.8*/
int main() {
    int c, nlbt;
    
    while ((c = getChar()) != EOF) {
        if (c == '\n')
            ++nlbt;
        if (c == '\t')
            ++nlbt;
        if (c == '\b')
            ++nlbt;
    }
    printf("%d\n",nlbt);
}

/*Exercise 1.9 */
int main() {
    int c;
    
    while ((c = getChar()) != EOF)
        if c.
}





































